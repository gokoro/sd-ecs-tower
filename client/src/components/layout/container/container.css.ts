import { style } from '@vanilla-extract/css'

export const BodyContainerStyle = style({
  padding: '0 1em',
  width: '100%',
})

export const TermContainerStyle = style({
  margin: 'auto',
  maxWidth: 900,
  paddingTop: 92,
})

export const ContentContainerStyle = style({
  padding: '2em 0',
})
